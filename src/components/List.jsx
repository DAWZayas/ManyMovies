/*

import React from 'react';

export default React.createClass({

solo borrar listas en las listas que custom true
  handleAddClick{},

  handleEditList{},

  handleRemoveClick{},

  render: function() {
    return (
      <div className="main">
        <h2> getTitle(id) </h2>
        <ul>
          {
        listas y su respectivo botón de borrar y editar, solo las custom true
          }
        </ul>
        //crear nuevas listas
      </div>
    );
  },
});
*/
